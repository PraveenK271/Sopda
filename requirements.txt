# Gemini ERP — core app dependencies (Python 3.14 venv)
# Install into the main project venv, NOT globally:
#   python -m venv venv
#   venv\Scripts\activate
#   pip install -r requirements.txt
#
# The OCR feature (Phase 3) runs in a SEPARATE Python 3.13 venv because
# PaddlePaddle has no wheels for 3.14 — see requirements-ocr.txt and README.md.

PySide6==6.11.1        # UI (Qt)
SQLAlchemy==2.0.50     # ORM / database access
reportlab==4.5.1       # PDF invoices
openpyxl==3.1.5        # Excel export
pyinstaller            # packaging into a Windows distribution (build.bat)
python-dotenv==1.0.1   # load GEMINI_DB_URL from .env (Phase 4 / MSSQL)
pyodbc==5.3.0          # SQL Server driver bridge (needs ODBC Driver 17/18 installed)
